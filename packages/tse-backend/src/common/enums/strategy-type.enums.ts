export enum StrategyTypeEnums {
  ARBITRAGE = 'arbitrage',
}

export enum StrategyInstanceStatus {
  CREATED = 'created',
  RUNNING = 'running',
  PAUSED = 'paused',
  STOPPED = 'stopped',
  DELETED = 'deleted',
}
